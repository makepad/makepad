[package]
name = "makepad-example-glyph-placement"
version = "0.1.0"
edition = "2021"
description = "Makepad exact glyph placement example"
license = "MIT OR Apache-2.0"

[dependencies]
makepad-widgets = { path = "../../widgets" }
