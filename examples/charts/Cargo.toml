[package]
name = "makepad-example-charts"
version = "1.0.0"
authors = ["Makepad <info@makepad.nl>"]
edition = "2021"
description = "Makepad charts example"
license = "MIT OR Apache-2.0"

[dependencies]
makepad-widgets2 = { path = "../../widgets2", version = "2.0.0" }
