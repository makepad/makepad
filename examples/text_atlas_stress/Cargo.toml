[package]
name = "makepad-example-text-atlas-stress"
version = "0.1.0"
edition = "2021"
description = "Text rows at several sizes plus a burst of fresh textures in one frame — the glyph-atlas regression stage"

[dependencies]
makepad-widgets = { path = "../../widgets", version = "2.0.0" }

[dev-dependencies]
makepad-test = { path = "../../libs/makepad_test", version = "0.1.0" }
makepad-zune-png = { path = "../../libs/zune/zune-png", version = "0.5.2" }
makepad-zune-core = { path = "../../libs/zune/zune-core", version = "0.5.1" }
