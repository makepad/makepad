[package]
name = "makepad-example-modal-footprint"
version = "1.0.0"
edition = "2021"
description = "Specimen page for the layout footprint of a Modal"
license = "MIT OR Apache-2.0"

[dependencies]
makepad-widgets = { path = "../../widgets", version = "2.0.0" }

[dev-dependencies]
makepad-test = { path = "../../libs/makepad_test", version = "0.1.0" }
