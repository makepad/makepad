[package]
name = "makepad-example-ddgo"
version = "1.0.0"
edition = "2021"
description = "Splash-only DuckDuckGo image search example"
license = "MIT OR Apache-2.0"

[dependencies]
makepad-widgets = { path = "../../widgets", version = "2.0.0" }
