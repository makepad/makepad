[package]
name = "makepad-example-bindgen-web"
version = "1.0.0"
edition = "2021"

[dependencies]
makepad-widgets = { path = "../../widgets" }

[target.'cfg(target_arch = "wasm32")'.dependencies]
js-sys = "0.3"
web-sys = { version = "0.3", features = ["Window", "Storage"] }
