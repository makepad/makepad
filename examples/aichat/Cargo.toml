[package]
name = "makepad-example-aichat"
version = "0.1.0"
authors = ["Makepad <info@makepad.nl>"]
edition = "2021"
description = "AI Chat example demonstrating makepad-ai library"
license = "MIT OR Apache-2.0"

[dependencies]
makepad-widgets = { path = "../../widgets", features = ["maps", "pdf"] }
makepad-code-editor = { path = "../../code_editor" }
makepad-ai = { path = "../../old/studio/ai" }
# NOTE: path dep (not git) because streaming-markdown-kit transitively
# depends on streaming-markdown-sanitizer which isn't yet published. To
# build this example, clone both sibling repos next to `makepad/`:
#     .../
#     ├── makepad/                       (this repo)
#     ├── streaming-markdown-kit/        (git@github.com:Project-Robius-China/streaming-markdown-kit)
#     └── streaming-markdown-sanitizer/  (TODO: publish)
streaming-markdown-kit = { path = "../../../../streaming-markdown-kit", features = ["mermaid"] }
