[package]
name = "makepad-derive-live"
version = "0.1.0"
authors = ["Makepad <info@makepad.nl>"]
edition = "2018"
description = "Makepad render derive macros"
license = "MIT"

[lib]
proc-macro = true

[dependencies]

[features]
default = ["nightly"]
nightly = []
