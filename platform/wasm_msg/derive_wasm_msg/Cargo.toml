[package]
name = "makepad-derive-wasm-msg"
version = "0.1.0"
authors = ["Makepad <info@makepad.nl>"]
edition = "2018"
description = "Wasm derive"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
makepad-macro-lib = { path = "../../macro_lib", version = "0.1" }

[features]
default = ["nightly"]
nightly = []
