[package]
name = "makepad-widgets-feedback"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Send feedback: a caption icon and a small panel that shows exactly what is sent (message, screenshot, system info)"

[dependencies]
makepad-widgets = { path = "..", default-features = false }
makepad-base64 = { path = "../../libs/base64" }
