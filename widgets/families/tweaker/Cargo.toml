[package]
name = "makepad-widgets-tweaker"
version = "2.0.0"
authors = ["Makepad <info@makepad.nl>"]
edition = "2021"
description = "Makepad widgets: The design overlay (Shift+F10) every Window carries, with its reflection and saved-theme store"
license = "MIT OR Apache-2.0"
homepage = "https://github.com/makepad/makepad/"
repository = "https://github.com/makepad/makepad/"

[dependencies]
makepad-widgets-core = { path = "../../core", version = "2.0.0" }
makepad-widgets-data = { path = "../data", version = "2.0.0" }
