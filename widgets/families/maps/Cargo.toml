[package]
name = "makepad-widgets-maps"
version = "2.0.0"
authors = ["Makepad <info@makepad.nl>"]
edition = "2021"
description = "Makepad widgets: The map view"
license = "MIT OR Apache-2.0"
homepage = "https://github.com/makepad/makepad/"
repository = "https://github.com/makepad/makepad/"

[dependencies]
makepad-widgets-core = { path = "../../core", version = "2.0.0" }
makepad-mbtile-reader = { path = "../../../libs/mbtile_reader", version = "1.0.0" }
i_overlay = { path = "../../../libs/i_overlay", version = "7.0.3", default-features = false }
makepad-fast-inflate = { path = "../../../libs/fast_inflate" }
