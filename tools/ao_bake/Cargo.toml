[package]
name = "makepad-ao-bake"
version = "0.1.0"
edition = "2021"
description = "Offline ambient-occlusion pre-baker for the stock model library"
license = "MIT OR Apache-2.0"

[[bin]]
name = "ao-bake"
path = "src/main.rs"

[dependencies]
makepad-render = { path = "../../libs/render" }
makepad-draw = { path = "../../draw" }
makepad-fast-inflate = { path = "../../libs/fast_inflate" }
