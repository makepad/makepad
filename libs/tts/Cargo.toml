[package]
name = "makepad-tts"
version = "0.1.0"
edition = "2021"
description = "Facade: Kokoro/system TTS now lives in makepad-ai-speech (aiarch.md §1)."
license = "MIT OR Apache-2.0"

[workspace]

[dependencies]
makepad-ai-speech = { path = "../ai/models/speech" }

[dev-dependencies]
makepad-voice = { path = "../voice" }
