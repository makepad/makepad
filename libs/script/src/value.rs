
#[derive(PartialEq, Eq, Clone, Copy)]
pub struct ScriptValue(u64);

//const VALUE_ID: Value = Value(0x0000_XXXX_XXXX_XXXX);
// ok so do we point to functions in objects?
// or do we directly interpret Object fields

impl ScriptValue{
    
}