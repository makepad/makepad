[package]
name = "makepad-scene"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Presentation worlds and pure geometry for Makepad renderers"

[dependencies]
makepad-math = { path = "../math", version = "1.0.0" }
