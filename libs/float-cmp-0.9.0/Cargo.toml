# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
edition = "2018"
name = "float-cmp"
version = "0.9.0"
authors = ["Mike Dilger <mike@mikedilger.com>"]
description = "Floating point approximate comparison traits"
documentation = "https://docs.rs/float-cmp"
readme = "README.md"
keywords = ["float", "comparison", "fuzzy", "approximate", "no_std"]
license = "MIT"
repository = "https://github.com/mikedilger/float-cmp"

[lib]
name = "float_cmp"
path = "src/lib.rs"
test = true
doctest = true
doc = true
[features]
default = []
# ratio/num-traits removed - kept as empty features to silence cfg warnings
ratio = []
num-traits = []
std = []

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = ["cfg(feature, values(\"num_traits\"))"]
