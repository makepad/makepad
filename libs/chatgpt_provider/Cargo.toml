[package]
name = "makepad-chatgpt-provider"
version = "0.1.0"
edition = "2021"
description = "Standalone ChatGPT subscription provider helpers for Makepad"
license = "MIT OR Apache-2.0"

[dependencies]
makepad-micro-serde = { path = "../micro_serde", version = "1.0.0" }
makepad-network = { path = "../../platform/network", version = "1.0.0" }
rand = "0.8.5"

