[package]
name = "makepad-game-render"
version = "0.1.0"
edition = "2021"
description = "Forward-pass renderer for Makepad Arcade (see game.md)"
license = "MIT OR Apache-2.0"

[dependencies]
makepad-draw = { path = "../../../draw", version = "2.0.0" }
makepad-game-sim = { path = "../sim" }
