[package]
name = "makepad-mesh-edit"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Checked polygon mesh authoring with stable identities"

[dependencies]
makepad-csg-math = { path = "../csg/csg_math" }
