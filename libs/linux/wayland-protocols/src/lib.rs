//! Pre-generated Wayland protocol bindings used by Makepad Linux.
//! This vendored crate is intentionally stripped to the protocol surface used by
//! the current Wayland backend.

#![allow(missing_docs)]
#![forbid(improper_ctypes, unsafe_op_in_unsafe_fn)]
#![cfg_attr(docsrs, feature(doc_auto_cfg))]

pub mod wp;
pub mod xdg;
