[package]
name = "makepad-ai-motion"
version = "0.1.0"
edition = "2021"
description = "HY-Motion family. Split out of libs/diffusion (lane T6, /aiarch.md)."
license = "MIT"

[dependencies]
makepad-ai-common = { path = "../common" }
makepad-ai-loader = { path = "../../loader" }
makepad-micro-serde = { path = "../../../micro_serde" }
makepad-ai-h3 = { path = "../h3" }
makepad-ai-flux = { path = "../flux" }
