[package]
name = "makepad-ai-rig"
version = "0.1.0"
edition = "2021"
description = "SkinTokens rig family. Split out of libs/diffusion (lane T6, /aiarch.md)."
license = "MIT"

[dependencies]
makepad-ai-common = { path = "../common" }
makepad-ai-loader = { path = "../../loader" }
makepad-gltf = { path = "../../../gltf" }
