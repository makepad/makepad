[package]
name = "makepad-ai-trellis"
version = "0.1.0"
edition = "2021"
description = "TRELLIS.2 mesh family. Split out of libs/diffusion (lane T6, /aiarch.md)."
license = "MIT"

[dependencies]
makepad-ai-common = { path = "../common" }
makepad-ai-loader = { path = "../../loader" }
makepad-gltf = { path = "../../../gltf" }
makepad-ai-h3 = { path = "../h3" }
