[package]
name = "makepad-ai-trellis"
version = "0.1.0"
edition = "2021"
description = "Native TRELLIS.2 and Pixal3D image-to-mesh models."
license = "MIT AND Apache-2.0"

[dependencies]
makepad-ai-common = { path = "../common" }
makepad-ai-loader = { path = "../../loader" }
makepad-gltf = { path = "../../../gltf" }
makepad-ai-h3 = { path = "../h3" }
