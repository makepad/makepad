[package]
name = "makepad-mbtile-reader"
version = "1.0.0"
edition = "2021"
description = "Minimal read-only SQLite parser for extracting tiles from MBTiles files"
license = "MIT OR Apache-2.0"

[dependencies]
