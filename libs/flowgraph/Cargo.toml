[package]
name = "makepad-flowgraph"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
makepad-widgets = { path = "../../widgets", default-features = false, features = ["rich_text"] }
