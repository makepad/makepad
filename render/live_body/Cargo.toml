[package]
name = "makepad-live-body"
version = "0.2.0"
authors = ["Makepad <info@makepad.nl>"]
edition = "2018"
description = "Makepad live body"
license = "MIT"

[features]
default = ["nightly"]
nightly = []

[lib]
proc-macro = true
